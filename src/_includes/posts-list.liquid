<ul class="blog-posts">
{% for post in posts %}
<li>
  <span>
    <i>{{ post.data.published_date | postDate }}</i>
  </span>
  <a href="{{ post.url }}">{{ post.data.title | escape }}</a>
</li>
{% endfor %}
</ul>